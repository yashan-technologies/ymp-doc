YMP provides the following two methods for installation:

- [Installation Deployment (default built-in database)](Installing and Uninstalling(default)): The default installation method, recommended for users to use this installation method first.

- [Installation Deployment (custom built-in database)](Installing and Uninstalling(custom)): For versions not supported by the built-in library, an external library can be used for installation.