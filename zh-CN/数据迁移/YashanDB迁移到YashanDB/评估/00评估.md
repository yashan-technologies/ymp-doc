评估阶段用于检查源端和目标端元数据的兼容适配性，YMP通过对象维度对异构数据库进行兼容度评估，在评估过程中可能会对创建对象的DDL语句进行一定程度的改写。

YMP提供了在线评估和离线评估两种评估方式，其中，在线评估的元数据可作为后续迁移阶段的源端信息输入；离线评估则只可用于执行评估任务。

对于YashanDB源端，只可进行在线评估。

采用在线评估时，为确保数据一致性，建议源端保持静默离线状态。
